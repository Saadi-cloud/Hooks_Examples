import React from 'react'
import Prop_data from './Prop_data'

const Props_exam_1 = (props) => {
  return (
    <div>
      <h1>{props.name}</h1>
      <h1>{props.age}</h1>
      <h1>{props.gender}</h1>
    </div>
  )
}

export default Props_exam_1
